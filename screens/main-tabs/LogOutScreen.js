import React from 'react';

import {Text, View} from 'react-native';

const LogOutScreen = () => {
    return (
        <Text>
            Log Out
        </Text>
    );
};

export default LogOutScreen;
